[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=orion_runner --cov=shared_rendering --cov-report=term-missing --cov-fail-under=80"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
