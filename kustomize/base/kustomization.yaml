apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: orion-newspaper

resources:
  - namespace.yaml
  - secret-quay.yaml
  - serviceaccount.yaml
  - service.yaml
  - imagestream.yaml
  - deployment.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: orion-newspaper-env
    namespace: orion-newspaper
    envs:
      - .env
